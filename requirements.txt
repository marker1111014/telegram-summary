# Runtime dependencies (local dev). Vercel installs from pyproject.toml — keep in sync.
python-telegram-bot==22.8
google-generativeai==0.8.5
python-dotenv==1.0.1
upstash-redis==1.4.0
fastapi==0.115.6
